# Video Duplicate Finder - Docker Configuration
# Copy this file to .env and customize the values for your setup

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================

# Language for the interface (en, de, es, fr, pt, zh-Hans)
VDF_LANGUAGE=en

# Similarity threshold (0-100, lower = more strict)
VDF_THRESHOLD=5

# Match percentage (0-100, higher = more strict)
VDF_PERCENT=96

# Use native FFmpeg binding for better performance (true/false)
# WARNING: Requires specific FFmpeg version, may cause crashes
VDF_USE_NATIVE_FFMPEG=false

# Hardware acceleration mode (none, vdpau, dxva2, vaapi, videotoolbox)
VDF_HARDWARE_ACCELERATION=none

# ===========================================
# SCAN CONFIGURATION
# ===========================================

# Include image files in scan (true/false)
VDF_INCLUDE_IMAGES=true

# Include subdirectories in scan (true/false)
VDF_INCLUDE_SUBDIRS=true

# Ignore read-only folders (true/false)
VDF_IGNORE_READONLY=true

# Primary scan directory (inside container)
VDF_SCAN_DIRS=/app/data/videos

# ===========================================
# STORAGE CONFIGURATION
# ===========================================

# Host directory for application data
VDF_HOST_DATA_DIR=./data

# Host directory for configuration files
VDF_HOST_CONFIG_DIR=./config

# Host directory containing videos to scan
VDF_HOST_VIDEOS_DIR=./videos

# Host directory for backups (used with backup profile)
VDF_HOST_BACKUP_DIR=./backups

# Database file path (inside container)
VDF_DATABASE_PATH=/app/data/ScannedFiles.db

# Application data directory (inside container)
VDF_DATA_DIR=/app/data

# Configuration directory (inside container)
VDF_CONFIG_DIR=/app/config

# ===========================================
# NETWORK CONFIGURATION
# ===========================================

# Port for future web interface
VDF_WEB_PORT=8080

# ===========================================
# RESOURCE LIMITS
# ===========================================

# CPU limit (e.g., 1.0, 2.5)
VDF_CPU_LIMIT=2.0

# Memory limit (e.g., 1G, 2048M)
VDF_MEMORY_LIMIT=2G

# CPU reservation (minimum guaranteed)
VDF_CPU_RESERVATION=0.5

# Memory reservation (minimum guaranteed)
VDF_MEMORY_RESERVATION=512M

# ===========================================
# LOGGING CONFIGURATION
# ===========================================

# Log level (Debug, Information, Warning, Error, Critical)
VDF_LOG_LEVEL=Information

# ===========================================
# BACKUP CONFIGURATION
# ===========================================

# Number of days to keep backups
BACKUP_RETENTION_DAYS=30

# ===========================================
# EXAMPLE ADDITIONAL SCAN DIRECTORIES
# ===========================================
# Uncomment and modify these lines in docker-compose.yml to add more scan paths:
# VDF_MOVIES_DIR=/path/to/your/movies
# VDF_SHOWS_DIR=/path/to/your/tv/shows
# VDF_DOWNLOADS_DIR=/path/to/your/downloads